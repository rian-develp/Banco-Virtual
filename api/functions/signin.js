import api from "../api";

export const signin = async ({setData}) => {
    // const response = await api.post("server/signin/", JSON.stringify(data))
    // .then(res => {
    //     return console.log(res.status + ' E um outro dado ' + res.headers);
    // }).catch((error) => {
    //     return console.warn(error)
    // });
    
}